<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirige vers index.php tous les appels aux routes inexistantes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
